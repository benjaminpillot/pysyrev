[pytest]
testpaths = tests
markers =
    slow:        requires spacy en_core_web_lg — run with: pytest -m slow
    integration: requires full ML stack (BERTopic, gensim, etc.) — run with: pytest -m integration
